# Bootcamp

This repo only contains a minimal framework. Write your code in 
`impl/include/impl` and `impl/src`, changes in other directories will be
ignored by grading system. You can only include files in
`framework/include/framework`. We may not present other files or presenting a
different version when grading.

